<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="oval">
    <!-- 提示行圆点（纯白）。运行时由 WeatherWidgetProvider 用 setColorFilter 染成建议级别色 —— 圆点是 ImageView，而 setColorFilter 只存在于 ImageView（View/TextView 都没有），这正是 v1.6.114 线上事故的成因：当时圆点是 TextView。 -->
    <solid android:color="#FFFFFF" />
</shape>
